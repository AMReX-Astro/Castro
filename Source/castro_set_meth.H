void set_castro_method_params
   (const Real& difmag, const Real& small_dens, 
    const Real& small_temp, const Real& small_pres, 
    const Real& small_ener, const int& do_hydro, 
    const int& hybrid_hydro, const int& ppm_type, 
    const int& ppm_reference, const int& ppm_trace_sources, 
    const int& ppm_temp_fix, const int& ppm_tau_in_tracing, 
    const int& ppm_predict_gammae, const int& ppm_reference_edge_limit, 
    const int& ppm_reference_eigenvectors, const int& hybrid_riemann, 
    const int& use_colglaz, const int& riemann_solver, 
    const int& cg_maxiter, const Real& cg_tol, 
    const int& use_flattening, const int& ppm_flatten_before_integrals, 
    const int& transverse_use_eos, const int& transverse_reset_density, 
    const int& transverse_reset_rhoe, const int& dual_energy_update_E_from_e, 
    const Real& dual_energy_eta1, const Real& dual_energy_eta2, 
    const Real& dual_energy_eta3, const int& use_pslope, 
    const int& normalize_species, const int& fix_mass_flux, 
    const int& allow_negative_energy, const int& do_sponge, 
    const Real& cfl, const Real& dtnuc_e, 
    const Real& dtnuc_X, const int& dtnuc_mode, 
    const Real& dxnuc, const int& do_react, 
    const Real& react_T_min, const Real& react_T_max, 
    const int& do_grav, const int& grav_source_type, 
    const int& do_rotation, const Real& rotational_period, 
    const Real& rotational_dPdt, const int& rot_source_type, 
    const int& rot_axis, const Real& point_mass, 
    const int& do_acc);
