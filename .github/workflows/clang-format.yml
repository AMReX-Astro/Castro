name: clang-format

on:
  pull_request:
    branches: development

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2

    - name: Run clang-format
      run: |
        pwd
        find Diagnostics Exec Source Util -maxdepth 4 -regex '.*\.\(cpp\|h\|H\)' | xargs clang-format -i -style=file

    - name: Check for changes
      run: |
        git add --all
        if [[ -z "$(git status --porcelain)" ]]; then
            echo "clang-format made no modifications"
            exit 0
        else 
            echo "clang-format has found formatting errors"
            git diff | tee clang-format-diff.txt
            exit 1
        fi
      
    - name: Archive git diff report
      uses: actions/upload-artifact@v1
      with:
        name: clang-format-diff
        path: clang-format-diff.txt
